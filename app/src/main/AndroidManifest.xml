<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.luciano.red">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:exported="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuPesquisaActivity" />
        <activity
            android:name=".SKUActivity"
            android:label="Perguntas Portifolio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DadosActivity"
            android:label="Dados" />
        <activity
            android:name=".CrudClienteActivity"
            android:label="Clientes" />
        <activity
            android:name=".CrudPerguntasActivity"
            android:label="Perguntas" />
        <activity
            android:name=".AtivacaoActivity"
            android:label="Perguntas Ativação" />
        <activity
            android:name=".GDMActivity"
            android:label="Perguntas Geladeira" />
        <activity
            android:name=".precoActivity"
            android:label="Perguntas Preço" />
        <activity
            android:name=".SoviActivity"
            android:label="Perguntas Sovi" />
        <activity
            android:name=".CadastroPerguntaActivity"
            android:label="Cadastrar Pergunta" />
        <activity
            android:name=".ClienteActivity"
            android:label="@string/title_activity_cliente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CadastroClienteActivity"
            android:label="Cadastrar Cliente">

        </activity>
    </application>

</manifest>